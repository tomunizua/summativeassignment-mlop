services:
  - type: web
    name: bxw-model
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: AWS_DEFAULT_REGION
        value: eu-north-1